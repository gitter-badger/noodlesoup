template(name="postOne")
  .twelve.wide.column
    h1 #{title}
    p #{body}
    br
    a(href="/t/#{tag}").ui.red.tag.label #{tag}
  .four.wide.column
    a(href="/a/#{author}")
      img(src="{{getGravatarUrl author}}").ui.avatar.image
      span #{author}